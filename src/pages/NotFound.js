const NotFound = () => {
  //this one is a globally defined css i.e. in index.js file
  return <div className="centered">
    Page Not Found!
  </div>
};

export default NotFound;

//This page is for the invalid URLs(unsupported paths)